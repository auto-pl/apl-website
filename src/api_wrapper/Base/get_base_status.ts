import { BaseStatus } from "../../interfaces/continent";

export const get_base_status = async (id: string): Promise<BaseStatus> => {
  // TODO: query API for BaseStatus
  // TODO: convert repsonse
  return {} as BaseStatus;
};
